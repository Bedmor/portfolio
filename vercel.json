{
  "buildCommand": "prisma generate && next build",
  "installCommand": "PRISMA_GENERATE_SKIP_AUTOINSTALL=1 npm install",
  "framework": "nextjs",
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "1"
  }
}
